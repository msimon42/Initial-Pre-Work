//Learned how to use Multiple Conditional Operators to check whether multiple conditions are true
function checkSign(num) {
  return (num === 0) ? "zero" : (num > 0) ? "positive" : "negative";
}

checkSign(10);
